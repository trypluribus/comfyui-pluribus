# Optional, local-only identity analysis. Install into the same Python
# environment that launches ComfyUI; no media or embeddings leave the machine.
Pillow>=9.5
numpy>=1.24,<3
opencv-python-headless>=4.8,<6
